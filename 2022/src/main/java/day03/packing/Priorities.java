package day03.packing;

/**
 * Offset values between ASCII codes and priority values
 */
public interface Priorities {

    int LOWERCASE_PRIORITY_OFFSET = -96;
    int UPPERCASE_PRIORITY_OFFSET = -38;
}
